@model SongIndexViewModel
@{
    ViewData["Title"] = "Songs";
}

@section Breadcrumbs {
    @await Html.PartialAsync("_Breadcrumbs")
}

<h1 class="page-title">Songs</h1>
<p class="page-lead">Scroll the full mock setlist and inspect each track in detail.</p>

<div class="form-actions mb-3">
    <a class="btn btn-primary" asp-controller="Song" asp-action="Create">Create song</a>
</div>

<div class="table-wrap">
    <table class="metal-table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Band</th>
                <th>Album</th>
                <th>Duration</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Songs.Count == 0)
            {
                <tr>
                    <td colspan="5">No songs available.</td>
                </tr>
            }
            else
            {
                @foreach (var song in Model.Songs.OrderBy(item => (item.Album?.Band?.Name) ?? string.Empty).ThenBy(item => item.Title))
                {
                    <tr>
                        <td>@song.Title</td>
                        <td>@(song.Album?.Band?.Name ?? "Unknown")</td>
                        <td>@(song.Album?.Title ?? "Unknown")</td>
                        <td>@TimeSpan.FromSeconds(song.DurationSeconds).ToString(@"m\:ss")</td>
                        <td>
                            <a class="inline-link" asp-controller="Song" asp-action="Details" asp-route-id="@song.Id">View details</a>
                            <a class="inline-link" asp-controller="Song" asp-action="Edit" asp-route-id="@song.Id">Edit</a>
                            <a class="inline-link" asp-controller="Song" asp-action="Delete" asp-route-id="@song.Id">Delete</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
